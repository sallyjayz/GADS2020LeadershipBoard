<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sallyjayz.gads2020leadershipboard">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_gads_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_gads_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ProjectSubmissionActivity"
            android:parentActivityName=".activity.LeadershipboardActivity">
            <!--<meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.LeadershipboardActivity" />-->
        </activity>
        <activity android:name=".activity.LeadershipboardActivity"
            android:label="@string/leader_board"/>
        <activity android:name=".activity.LaunchScreenActivity"
            android:theme="@style/Splash">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>